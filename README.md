<!-- export CC=gcc
export EGREP="grep -E"
export AR="ar"
export RANLIB="ranlib"
export WINDRES="windres"
CC=gcc ./configure --enable-64bit --prefix=$(pwd)/../../dist --disable-seh

make
make install

sudo apt install mingw-w64 -->